name: TODO check
on:
  push: { branches: "alex/maybe-fix-ci" }
env:
  PROJECTS_SECRET: ${{ secrets.PROJECTS_SECRET }}

jobs:
  check_todos:
    runs-on: ubuntu-latest
    steps:
    - name: TODO to Issue
      uses: alstr/todo-to-issue-action@v4.13.1
      with:
        TOKEN: ${{ secrets.PROJECTS_SECRET }}
